{
  "name": "discraft-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.3.6",
  "scripts": {
    "fmt": "prettier . -w  --cache --cache-strategy=metadata --cache-location=./.prettier-cache"
  },
  "devDependencies": {
    "prettier": "^3.8.0",
    "turbo": "^2.7.4"
  }
}
