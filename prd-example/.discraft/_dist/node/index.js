// Minified output to run from the Node adapter builder.
// `discraft start` would know to run this file because the `nodeAdapter` would return the dist directory in the config, which the CLI can load and understand
// If we used the Vercel adapter, it might make a ../vercel/index.js file
// Or for example executableAdapter would be ../executable/(binaries)
