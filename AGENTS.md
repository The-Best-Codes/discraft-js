This monorepo uses Bun. Use bun to install packages (do not edit package.json files manually to install packages).
This monorepo uses Turborepo. Use turbo to run tasks in parallel, for example `turbo build` to build packages.

## Rules

- Document as you go. Any time you add new functionality, update the documentation when you're done.
